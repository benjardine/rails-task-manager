<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <%= link_to "Delete",
      destroy_task_path(task.id), method: :delete, data: { confirm: "Are you sure?" } %></li>
  <% end %>
</ul>
